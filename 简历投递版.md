# Enterprise RAG 企业知识库问答系统 | 独立开发

**技术栈：** Python、FastAPI、Vue 3、LangGraph、Milvus、PostgreSQL、Redis、Docker、SSE、BGE-M3、MinerU、Qwen Reranker

### 项目介绍

- 面向企业制度、流程、项目资料和产品手册，搭建从多格式资料入库、混合查找，到带来源回答和流式交互的完整知识库问答系统。

- **围绕“文件找到了、关键内容却被切散”的问题完成优化闭环：** 在固定的 300 道英文题中，逐题审计发现部分正确文件已被找到，但答案所需的标题、规则、表格或限制条件落在同一文件的不同小段；据此将原有按长度切资料的方式升级为按 Markdown 标题和内容结构切分，保留标题、列表、表格和代码块。在相同的 1,022 篇资料、模型和检索设置下，题目所需标准资料全部进入最终内容的比例 **81.25% -> 83.68%**，回答通过率 **62.00% -> 69.33%**，新增答对 **22 道**。

- 设计 L1/L2/L3 父子资料组织：仅将最小资料段写入 Milvus 做精确查找，上层章节存入 PostgreSQL 并由 Redis 缓存；同一章节命中多个小段时自动补回父级上下文，兼顾“找准一条规则”和“看全前提条件”。

- 构建“混合查找 -> 章节补全 -> 再排序 -> 回答”链路：结合 BGE-M3 语义查找、Milvus 原生 BM25 关键词查找和 RRF 融合，对前 30 段候选用 Qwen Reranker 再排序，最终提供 8 段资料；混合查找失败时自动降级为语义查找，再排序服务异常时保留原有排序并记录原因，避免单点服务故障中断回答。

- 优化复杂问题的成本与检索路径：能由规则识别的单一事实问题直接查资料，其余问题由快速模型一次完成复杂度判断和 2-4 个子问题规划；通过 LangGraph 并行检索各子问题并去重合成，资料不足时最多改写一次查询，再根据证据进入回答、澄清或拒答路径。

- 支持 PDF、Word、PPT、Excel 和图片统一入库：通过 MinerU 转为 Markdown，保留原文件、转换结果和解析清单；同名资料先在暂存区完成解析与分块校验，再替换旧索引，避免新文件处理失败影响已生效资料。

- 建立按请求隔离的 SSE 流式链路：后端实时推送检索、评分、改写等状态，前端展示第一次资料池、章节补全、排序变化、最终来源和实际执行路径，并支持用户中断生成，便于用户理解回答依据和开发者复盘问题。

### 评测与优化

- 构建 300 道可复现的英文评测：固定 722 篇标准资料与 300 篇干扰资料，共 1,022 篇；保存逐题第一次资料池、排序变化、最终 8 段资料、回答、判分和异常，区分“资料有没有找到”“关键内容有没有进入最终资料”“模型有没有答对”和“服务有没有报错”。

- 在同一资料库和同一题集上完成旧切分与 Markdown 结构化切分的完整对照，两个版本均 300/300 完成、系统异常为 0；平均资料覆盖率 **85.89% -> 88.62%**，回答通过率 **62.00% -> 69.33%**。同时保留独立 Milvus、PostgreSQL、Redis 环境和每次补跑记录，避免实验数据影响业务知识库。

### 面试准备：主问题与追问

> 本节用于面试准备，投递简历时可删除。二级标题是面试官最可能先问的主问题；三级标题是该问题下常见的继续追问。

## 1. 你能从用户提问开始，把整条 RAG 链路讲清楚吗？

用户提问后，系统先判断是简单问题还是需要拆解的复杂问题；复杂问题会拆成 2-4 个小问题分别找资料。每个问题先用语义查找和关键词查找并行找出候选资料，再用 RRF 合并排序；如果多个小段来自同一章节，会补回相应上下文。接着对前 30 个候选资料再次排序，取最终 8 段交给回答模型，并把来源、排名和执行过程实时展示给用户。

### 追问：你怎么判断一个问题该拆分？如果模型把简单问题误判成复杂问题怎么办？

先用快速规则处理明确的简单问题；不能确定时，再让快速模型返回结构化结果，只允许 `simple` 或 `complex` 两种结论。复杂问题必须给出 2-4 个可独立查找的小问题，否则该次规划不通过。拆分结果、判断理由和每个小问题都会写进 trace；如果拆得过细，后续合成阶段会按 `chunk_id` 去重，避免同一资料重复塞进回答上下文。

我不会把“多拆”当成默认优化。拆分会增加模型调用和资料噪声，所以适合多条件、多资料来源的问题；单点事实问题直接按原问题查找更短、更稳定。

### 追问：为什么复杂问题的每个子问题不允许继续改写？会不会错过资料？

主问题最多允许一次改写，子问题不再改写。这是为了防止“一个问题拆成 4 个，4 个又各改写一次”导致请求数和等待时间成倍增加，也让失败时很难判断到底是拆分还是改写造成了变化。子问题如果有部分资料，会先交给合成节点和其他子问题的资料一起判断；完全没有相关资料才停止该分支。

### 追问：Step-back 和 HyDE 分别是什么？为什么它们不能被当成回答证据？

Step-back 是把用户的具体问法先换成更上层、更通用的问题，用来找到可能没写同样词的资料；HyDE 是让模型生成一段“可能长什么样”的检索用文字，再用它找相近资料。两者的作用都只是帮助系统换一种问法查资料，不是企业原始文档，所以回答只能引用真正查回来的资料，不能把改写内容本身当事实。

### 追问：如果回答模型说得很像对，但没有来源支持，你的系统如何处理？

回答提示要求所有事实只来自最终查回的资料，并在回答中按资料编号引用；改写问题和 HyDE 生成文本不能作为来源。证据评分认为资料不够时，流程会走一次改写、向用户澄清，或直接说明知识库没有可靠资料，而不是把相关但不充分的内容直接拼成确定结论。

## 2. 为什么只把 L3 小段写进 Milvus，而不把整篇文档都写进去？

小段更容易准确匹配用户问题，例如一条规则、一个版本号或表格中的某个字段；整篇文档更适合帮助模型理解前提和限制条件。我的做法是用 L3 负责“找准”，命中后通过父子关系补回 L2/L1 负责“看全”。这样既避免把整篇文档反复命中，也不会只给模型一句脱离上下文的话。

### 追问：L1、L2、L3 具体有多大？为什么要三层而不是只用一个固定长度？

当前目标大小约为 L1 2,400 字符、L2 1,600 字符、L3 800 字符，并分别保留约 400、200、100 字符重叠。L3 用于精确查找，L2/L1 用于恢复更完整的上下文。三个层级是逐层嵌套生成的，不是把同一段文字机械复制三遍，因此一个 L3 能稳定找到它所属的 L2 和 L1。

### 追问：什么时候会把多个小段换成上层章节？会不会把太多无关内容一起带进来？

当前规则是：同一个父块下至少命中 2 个子块，才把它们上卷成父块；这个过程会先从 L3 到 L2，再判断是否需要从 L2 到 L1。这样做的目的不是“命中一个小段就把整章都塞进去”，而是当系统已经有多个信号指向同一章节时，再补齐上下文。上卷后的内容仍会进入再次排序，最后只保留 8 段。

### 追问：分块后如何保证一段资料能回到原文，而不是只剩一段脱离出处的文本？

每个块都保存稳定的 `chunk_id`、父块和根块 ID、文件名、页码、原文起止位置、标题路径、结构类型及相邻块 ID。回答页面和评测候选轨迹都能顺着这些字段回到文件、章节和原文位置。这个设计既用于给用户展示来源，也用于复盘“正确文件出现了，但真正需要的那句话在哪一步丢了”。

## 3. 为什么同时做语义查找和关键词查找，还要再做一次排序？

语义查找擅长处理说法不同但意思相近的问题，关键词查找对产品名、错误码、版本号和配置项更稳。两路结果用 RRF 合在一起，先尽量把可能有用的资料找全；再排序则是在这批资料里重新挑选最贴合问题的内容。简单说，前一层负责“别漏掉”，后一层负责“别把不该看的资料交给模型”。

### 追问：为什么 RRF 用排名合并，而不是直接把两路分数相加？

语义查找的分数和关键词查找的分数不是同一个尺度，直接相加容易出现一边的分数范围更大、把另一边压掉的问题。RRF 只看各自的名次：一份资料在两路都靠前，就会得到更靠前的综合位置。它适合先把两种能力合成一个候选池；真正决定最终 8 段的仍是后面的再次排序。

### 追问：再次排序失败时为什么不直接报错？如何知道它到底有没有生效？

再次排序是提高候选质量的步骤，不应该让它的接口故障把整条问答链路打断。若上游超时、返回错误或结果为空，系统保留原先的融合排序继续回答，并在 trace 写入 `rerank_error`；正常时会记录模型名、耗时、输入候选数、再次排序分数和最终名次。因此可以明确区分“排序模型实际工作了”和“这次走了降级路径”。

## 4. 为什么候选资料取 30 段，最后只给模型 8 段？这两个数字怎么来的？

30 段是第一次查找后的资料池，给再次排序留下足够选择空间；最终保留 8 段是为了控制模型看到的无关内容和上下文成本。它们不是随回答临时变化的，而是作为固定参数进入逐题评测。调这类数字不能只看某一道题，而要在固定题集上同时看资料是否找全、最终资料是否完整、回答是否通过和耗时。

### 追问：如果面试官要求你证明这两个数字最优，怎么回答？

30 段是给再次排序留出的资料池，8 段是控制最终上下文噪声和成本的上限。它们是当前固定评测配置，而不是宣称放之四海皆准的最优数字。我会说明：任何调整都要只改变这一个数，在同一题集上同时比较第一次资料池是否找全、最终 8 段是否保留关键信息、回答通过率和耗时；不能因为一两道题变好就改线上参数。

## 5. 为什么要用 LangGraph？这算多 Agent 吗？

我使用 LangGraph 是为了把“判断问题复杂度、拆分小问题、查资料、资料不足时改写、回答或拒答”组织成可追踪的状态流程。它不是为了堆多个 Agent：企业知识库问答的核心问题是让每一步可控、可复查，单一流程图比多个角色互相对话更适合。只有任务需要多个独立工具或角色分工时，我才会考虑多 Agent。

### 追问：这个项目为什么不直接用多 Agent？单图流程有什么工程价值？

项目中的“子问题分支”是为了把复杂问题的资料查找并行化，不是让多个角色自由讨论。企业知识库问答更需要每一步可预测、能设调用预算、能保存证据路径；LangGraph 的状态图把分类、检索、评分、改写、澄清和合成明确连起来。只有出现真正独立的工具协作任务，例如检索、数据库查询和业务操作需要不同权限与计划时，多 Agent 才有更明确的收益。

## 6. 你说把按长度切分升级为按 Markdown 结构切分，具体解决了什么问题？

原来的切法容易把标题、规则列表、表格和后面的限制条件切到不同小段里。用户的问题往往需要把这些内容一起看到，文件虽然找到了，真正的关键信息却没有进入模型最后看到的资料。新做法先以 Markdown 标题划分章节，列表、表格、代码块保持完整，只有普通长段落再继续切分，并记录每段的标题路径、原文范围和相邻关系。

### 追问：标题、列表、表格和代码块如果比块大小还长，系统怎么处理？

先把它们当成一个不可随意拆开的结构单元：连续列表、表格主体和代码围栏不会被从中间切开；正常大小时与同标题下的其他内容组合成块。只有普通长段落继续按自然分隔符递归切分。这样宁可出现一个稍大的结构块，也避免把表头和表格行、代码和说明、规则标题和规则正文拆成无法理解的碎片。

### 追问：为什么没有直接把“按内容变化切分”的语义分块设为默认方案？

我做过单独对照：两组都使用同样的 1,022 篇资料和 500 道开发题，语义方案只对过长普通段落按内容变化选择断点，标题、列表、表格和代码块仍按 Markdown 结构保护。结果是语义方案的资料全到位率只多 0.21 个百分点，但回答通过率从 69.40% 降到 67.20%，少答对 11 道。因此当前默认保留 Markdown 结构切分。这个结果只能说明这套语义切法没有带来净收益，不能说明按内容变化切分永远没有价值。

## 7. 这个分块优化怎么证明真的有效，而不是模型刚好这次答对了？

我没有拿单题演示下结论，而是在同一份 1,022 篇资料和同一批 300 道英文题上，完成旧切分与 Markdown 结构化切分的完整对照。模型、向量模型、再次排序、候选数量、最终 8 段数量和提示词保持一致，两个版本都没有系统异常。题目所需标准资料全部进入最终内容的比例从 81.25% 提升到 83.68%，回答通过率从 62.00% 提升到 69.33%，多答对 22 道；每题仍保留第一次资料池、再次排序和最终 8 段内容，可回看提升发生在哪一步。

### 追问：为什么这次可以直接比较 300 道，而不是只看少数难题？

因为两次运行使用的 722 篇标准资料、300 篇干扰资料和 300 道题逐项一致，文档内容哈希和题目清单也一致；模型、查找和再次排序设置保持一致，唯一改变的 RAG 逻辑是资料怎么切开和组织。因此可以直接用 300 道完整对照作为对外汇报结果。

### 追问：标准资料全到位只提升了 2.43 个百分点，为什么答对率能提升 7.33 个百分点？

“资料全到位”只看题目需要的文件是否都进了最终 8 段，不等于模型已经看到了每个关键句和限制条件。结构化切分还会改变标题、表格和规则前后关系在最终内容中的保留方式，所以同一份文件即使原来已出现，也可能从“不好用”变成“能直接回答”。这组总指标说明改善不只来自多找到文件；但总表不能证明每一道题的具体原因，必须回看该题第一次资料池、最终 8 段和回答，才能下结论。

### 追问：哪些题提升最明显，哪些题仍然困难？

条件限制较多的 18 道题，答对数从 5 道升到 11 道；基础查询的 105 道题从 79 道升到 87 道；需要在同一文件内串联内容的 24 道题从 17 道升到 19 道。另一方面，要求列全多项信息的 12 道题、项目关联题和概念理解题仍然偏难。样本较小的题型不能只看百分比下结论，所以我会同时报题数，并把它们作为下一轮逐题排查对象，而不急着继续调切分长度。

### 追问：这组分块数据能说明什么，不能说明什么？

它说明结构化切分在这 300 道固定题上带来了净提升：更多题的所需资料完整进入模型最终看到的内容，回答也多答对了 22 道。它不能说明所有问题都由切分解决，例如需要列全信息、项目关联和概念理解类题仍有其他原因；后续优化要继续看资料是在第一次查找时没找到，还是后来被再次排序筛掉。

## 8. 为什么不能只看“答案通过率”？你还看了哪些指标？

答案错可能有三种完全不同的原因：资料根本没找到、资料找到了但没有进入最终 8 段、资料已经完整但模型没有组织好答案。因此我分开记录标准文件是否进入第一次资料池、关键内容是否进入最终资料、回答是否通过和是否有系统异常。这样优化时不会把服务超时误当成模型能力问题，也不会把“文件名出现”误认为已经有了完整证据。

### 追问：你怎样区分“文件找到了”和“证据真的足够”？

文件出现在第一次资料池，只能证明系统找到了这份文件；题目所需的具体版本号、条件或表格行可能仍在文件的其他位置。为此评测记录分开看：标准文件是否进入第一次资料池、资料是否进入最终 8 段、回答是否通过、是否出现系统异常。只有逐题核对关键内容时，才能说关键事实真正到位。

### 追问：自动判分可靠吗？人工复核在什么情况下介入？

自动判分适合稳定地跑完大量题，但对“回答是否覆盖了全部限定条件”“多余一句是否真的构成错误”可能有边界情况。因此自动结果会保留判分理由，并把不确定结果、资料完整但回答失败、回答通过但资料不足以及系统异常放进人工复核队列。人工复核的作用是校正具体题的结论，不是用几道主观样例替代整套固定评测。

## 9. 完整对照完成后，哪些问题仍然没有解决？

结构化切分有效，但没有解决所有类型的问题。要求把多项内容列全的 12 道题，标准资料全部进入最终内容的数量从 4 道变为 2 道；项目关联的 24 道题答对数只从 2 道变为 3 道；概念理解的 75 道题答对数保持 47 道不变。这说明剩余问题不应一概归为“切得不够好”，可能发生在最开始找资料、从候选里筛资料，或模型拿到资料后的组织回答阶段。

### 追问：如果再做下一轮优化，你会优先改什么？为什么不直接继续调分块长度？

先从“要求列全多项信息”和“项目关联”两类题里抽取固定样本，逐题看三件事：第一次找出的资料里有没有标准文件，最后 8 段里有没有留下关键内容，模型是否漏写或混入了不该写的内容。只有确认同一种失败反复出现，才只改对应的一处：最开始没找到就改问题表达或查找方式，后面被筛掉就检查再次排序，资料齐全仍答错才改回答约束。这样不会为了少数题去盲调全局切分长度。

## 10. 同名文档更新时，怎样避免新文件解析失败导致知识库空掉？

新文件不会一上传就覆盖旧资料。系统先在暂存区域完成文件转换、分块和校验，只有新版本准备成功后才替换旧索引；因此解析失败时，旧资料仍然可以继续被查到。这是为了解决资料更新过程中的可用性问题，而不是只追求一次写入成功。

### 追问：上传同名文件时，PostgreSQL、Milvus、Redis 三处如何保证一致？

当前更新流程先在暂存区完成文件解析与分块校验，成功后才进入替换旧索引的步骤，因此可以避免“新文件解析失败就先把旧资料删掉”的问题。面试中我会如实说明：目前已保护解析阶段和可追踪的入库流程；跨 PostgreSQL 与 Milvus 的完整分布式事务回滚还不是现有实现。若继续产品化，我会用版本号或状态位先让新版本完整就绪，再切换可见版本，并保留旧版本用于回滚。

## 11. 评测时为什么要用独立的 Milvus、PostgreSQL 和 Redis？

评测会反复入库、重跑和记录大量中间状态，如果直接写业务知识库，会混入测试资料、污染缓存和影响线上查找结果。因此每轮实验都有独立的向量集合、评测数据库和 Redis 前缀；资料、模型、题目和配置都固定，只允许修改一个优化点，结果才可以公平比较和复查。

### 追问：为什么评测重试只补跑异常题？怎样保证补跑没有偷偷改变实验条件？

正常完成的题直接复用，只有模型调用或自动判分没有得到结果的题重跑。新的运行 ID 会保存来源运行、原始结果哈希、复用题目和补跑题目清单；资料、题目、模型、查找、再次排序和提示词都必须与来源一致。若确实只是等待时间不够，只允许增加等待时间或降低并发，并把变化写进配置；不能借补跑顺手换模型或改检索参数。

## 12. 系统异常题怎么处理，为什么不能把它们当成答错？

模型接口超时、远端断开或自动判分超时，只说明服务没有给出可用结果，不等于回答质量差。本次旧切分的 300 道运行曾出现这类问题，我保留每次异常记录，只补跑异常题，并把并发和等待时间的调整写进新的配置。最终形成 300/300 完成、系统异常为 0 的结果，才用来与 Markdown 组比较。这样不会把运行问题算成优化收益，也不会把异常题直接当成答错。

### 题型参考

- 牛客：[RAG 常见面试问题](https://www.nowcoder.com/feed/main/detail/4366a6c83d1446ee844f4ab8af3fa25c)、[Agent 面试汇总](https://www.nowcoder.com/discuss/878352209799364608)
- 知乎：[RAG 面试题汇总](https://zhuanlan.zhihu.com/p/2029999895302628181)
- 技术社区：[Advanced RAG techniques](https://www.meilisearch.com/blog/rag-techniques)
