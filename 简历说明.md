# Enterprise RAG 企业知识库问答系统 | 独立开发

**技术栈：** Python、FastAPI、Vue 3、LangGraph、Milvus 2.5、PostgreSQL、Redis、Docker、SSE、BGE-M3、MinerU、Qwen Reranker

面向企业制度、流程、项目资料、产品手册和 FAQ 的智能问答系统。支持多格式资料入库、混合检索、带来源回答和流式交互；保留从资料查找至最终回答的完整记录，可定位资料在哪一步丢失或模型为何答错。

### 项目实现

- **搭建多格式资料入库与安全更新链路：** 将 PDF、DOCX、PPTX、XLSX 和图片通过 MinerU 统一转换为 Markdown，保留原文件、转换结果和解析清单；同名资料先完成暂存解析与分块校验，再替换旧索引，避免新文件处理失败影响已生效资料。

- **设计三级资料组织与混合检索：** 构建 L1/L2/L3 父子块，仅将 L3 写入 Milvus 用于精确查找，L1/L2 存入 PostgreSQL 并用 Redis 缓存；结合 BGE-M3 语义查找、Milvus BM25 关键词查找和 RRF 融合，命中后自动补回所属章节内容。

- **实现资料再次排序与复杂问题处理：** 对前 30 个候选资料使用 `Qwen/Qwen3-Reranker-4B` 再次排序，最终选择 8 段内容回答；基于 LangGraph 将复杂问题拆成 2-4 个小问题分别查找，资料不足时执行一次查询改写，并按资料状态进入回答、澄清或拒答路径。

- **建设可观测的流式问答体验：** 后端通过 SSE 推送回答和处理状态，前端展示第一次资料池、章节补全、再次排序和最终 8 段资料及来源；记录各阶段排名、得分和实际执行路径，支持快速复查问题。

### 评测与优化

- **建立可重复的英文评测体系：** 最终评测配置固定 722 篇标准证据文档与 1,000 篇干扰文档，共 1,722 篇资料；固定题目、模型、Embedding、提示词、候选数量、再次排序和上下文补全参数，每轮仅修改一个变量并保存配置快照、逐题 JSONL、候选资料轨迹和人工复核记录。

- **实现逐题问题定位闭环：** 评测直接调用真实 `/chat` 链路，保存回答、第一次资料池、再次排序前后结果、最终 8 段内容、判分理由、耗时和系统异常；审计 97 道资料链路可疑题后，定位到 30 道标准文件未完整进入第一次资料池、41 道文件已找到但关键内容落在同一文件的其他小段。

- **完成 Markdown 结构化分块优化并验证效果：** 将原项目的普通递归切分 `recursive_l1_l2_l3` 升级为 `markdown_header_recursive_v1`：先按 Markdown 标题组织章节，再递归处理普通长段落，保留列表、表格和代码块完整性，并记录标题路径、原文范围、相邻段落和父子关系。在 30 道“同一文档关键信息被切散”的难题中，排除系统异常后对 24 道同题比较：资料全量到位率 **8.33% -> 54.17%**，平均资料覆盖率 **14.70% -> 59.14%**，回答通过率 **4.17% -> 33.33%**，新增答对 **7 道**。

- **实现隔离的优化实验环境：** 为每轮对照建立独立的 Milvus collection、PostgreSQL 和 Redis 命名空间，避免评测资料写入默认业务知识库；支持只补跑系统异常题，保证原始结果与重试记录均可复查。
