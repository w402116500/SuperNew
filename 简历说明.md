# 简历说明

## AI 智能知识检索系统 - 独立开发

**技术栈：** Python、FastAPI、Vue 3、LangGraph、Milvus 2.5、PostgreSQL、Redis、Docker、SSE、BGE-M3

**项目简介：** 基于 RAG 的企业知识库智能问答平台，支持多格式文档上传、解析、三级分块、混合检索和流式问答。系统通过检索过程追踪、来源引用与固定评测集，保证回答可回溯、可分析、可验证。

## 核心职责

1. 基于 FastAPI、PostgreSQL、Redis 和 JWT 实现用户认证、会话隔离及管理员文档管理；支持 Markdown、PDF、Word、Excel、HTML、TXT 等格式上传，并通过后台任务执行“解析 -> 分块 -> 向量化 -> 入库”，上传同名文档时自动清理旧索引，避免新旧资料混用。

2. 设计 L1/L2/L3 三级分块与父子关联机制：L3 叶子块写入 Milvus 用于精准召回，L1/L2 父块存储于 PostgreSQL 并使用 Redis 缓存；命中多个相邻叶子块时自动合并父级上下文，降低长文档检索碎片化导致的回答缺失。

3. 基于 Milvus 原生 BM25 Function 实现“稠密向量 + 稀疏关键词”的 Hybrid Search，并通过 RRF 融合排序，兼顾语义相近问题与专有名词、版本号、配置项等精确匹配场景；混合检索异常时自动降级为纯向量检索，提升服务稳定性。

4. 基于 LangGraph 构建 RAG 检索流程：简单问题直接检索，复杂问题由快速模型拆分为子问题并行检索和证据判断；资料不足时在 Step-back 与 HyDE 中选择一种进行单次查询改写和复检，最终仅依据召回证据生成带来源编号的回答。

5. 实现 SSE 流式问答、前端停止生成、检索过程实时展示与 RAG Trace 记录；前端可查看召回资料、候选数量、自动合并、证据判断、查询改写及可选 Rerank 是否实际生效，方便定位“没找到资料”与“找到资料但未正确作答”等不同问题。

6. 构建固定 RAG 评测流程：评测脚本通过真实聊天接口逐题请求，记录回答、召回资料、排名、耗时，并生成 JSON 汇总和 Markdown 成绩单；使用固定题集对检索完整性、关键事实覆盖、错误回答和延迟进行重复验证，为后续优化提供基线。
